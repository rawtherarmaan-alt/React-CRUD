# React CRUD

## 1.create a project(open git bash in react workspace)
`npm create vite`

## 2.instsall the node modules(open git bash inside the project)
`npm install`

## 3.third party pakages(git bash inside the project)
- react-router - to handle react routes
- bootstrap - css library
- axiou - to axious api requtes
- react-tostify - custom toast alerts

`npm inatall --save react-router axious react-toastify bootstrap`

## 4.Run the project
`npm run dev`
